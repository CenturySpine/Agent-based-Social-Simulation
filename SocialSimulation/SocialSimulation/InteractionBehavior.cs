using System;

namespace SocialSimulation
{
    public class InteractionBehavior : IEntityInteractionBehavior
    {
        public void Behave(Entity entitySource, Entity entityTarget, GlobalSimulationParameters simulationParams, Random random, InteractionService interactionService)
        {
        }
    }
}